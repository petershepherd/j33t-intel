{
  "name": "@j33t-intel/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "db:migrate": "wrangler d1 migrations apply j33t-intel-db"
  },
  "dependencies": {
    "@j33t-intel/shared": "workspace:*",
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "wrangler": "^4.0.0",
    "@cloudflare/workers-types": "^4.20250109.0"
  }
}
